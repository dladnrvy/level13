<html>
	<body>
<%= form_tag '/new' do %>
아이디 : <input type="text" name="user_id"><br>
비밀번호 : <input type="password" name="password"><br>
  이름: <input type="text" name="name"><br>
  성별: <input type="text" name="gender"><br>
   <input type="submit" value="가입하기">
 <% end %>
 
 </body>
 <script>
window.onload = function () {
};
	var form = document.getElementById('form');


	// 취소버튼 생성
 	var cancel = document.getElementById('cancel');
 	
 	cancel.onclick = function(){
	   location.href = '/';
	 }
	 
	 //비밀번호와 비밀번호 확인 체크, 유효성검사 변수
	 var password = document.getElementsByName('password').value;
	 var password_confirmation = document.getElementsByName('password_confirmation').value;
	 //유효성검사 변수
	 var email = document.getElementById('email').value;
	 var name = document.getElementsByName('name').value;
	 var from = document.getElementsByName('from').value;
	 
	 form.onsubmit = function(){
	 	alert(email);
	 	//유효성검사
	 	if(typeof password == 'undefined' || typeof password_confirmation == 'undefined'  || typeof email == 'undefined' || typeof name == 'undefined' || typeof from == 'undefined'){
	 		alert('빈 값이 있습니다. 확인해주세요.');
	 		alert(password);
	 		alert(name);
	 		return false;
	 	 }
	 	 
	 /*
	  if(password_confirmation != password){
			  alert('비밀번호를 다시 확인해주세요!');
			  return false;
				  }*/
	 
	 		
	 		
	 	
	 }

</script>
</html>
